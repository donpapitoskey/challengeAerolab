import styled from 'styled-components';
import {theme} from 'src/utils/theme';

const {gray85} = theme.colors;

export const CatalogContainer = styled.main`
  min-height: 100vh;
  background-color: ${gray85};
`;
